<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/primary"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.DetailEventActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/iv_detail_imgevent"
                style="@style/imageEvent"
                android:contentDescription="@string/image_event"
                android:src="@drawable/events_one" />

            <TextView
                android:id="@+id/tv_detail_headevent"
                style="@style/tvHeadevent"
                tools:text="Headline Event" />

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:id="@+id/tv_detail_author"
                    style="@style/tvAuthor"
                    tools:text="Author" />

                <TextView
                    android:id="@+id/tv_detail_date"
                    style="@style/tvDate"
                    tools:text="Date" />
            </RelativeLayout>

            <TextView
                android:id="@+id/tv_detail_content"
                style="@style/tvDetailcontent"
                tools:text="Event Content" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>